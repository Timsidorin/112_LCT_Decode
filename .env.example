# Скопируйте в .env в корне репозитория перед docker compose up:
#   cp .env.example .env
#
# Секреты приложения (S3, AI, JWT) — в backend/.env (см. backend/.env.example)

# PostgreSQL (сервис db)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=timofeymac

# Домен для nginx/certbot (опционально, для первичной настройки SSL)
DOMAIN=ai-csis.ru
