# Локальная разработка: проброс PostgreSQL и Redis на хост
# Запуск: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db redis

services:
  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"
